Django>=5.1,<5.2
djangorestframework>=3.15
psycopg2-binary>=2.9
django-environ>=0.11
django-cors-headers>=4.0
Pillow>=10.0
requests>=2.31
fpdf2>=2.7
uharfbuzz>=0.39
# Read the embedded fonts' cmaps so a glyph we cannot draw is caught, not
# silently dropped from the invoice (a name in an unsupported script would
# otherwise render as a blank field).
fonttools>=4.50
djangorestframework-simplejwt>=5.3
# Deployment (Render + Supabase pooler)
dj-database-url>=2.1
gunicorn>=22.0
whitenoise>=6.6
